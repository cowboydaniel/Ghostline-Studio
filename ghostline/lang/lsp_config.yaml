# Default language server definitions for Ghostline Studio
servers:
  python:
    primary:
      # NOTE: Standard pyright does NOT support semantic tokens (syntax highlighting).
      # For semantic token support, use basedpyright instead:
      #   pip install basedpyright
      # Using basedpyright for semantic token support:
      command: "basedpyright-langserver"
      args: ["--stdio"]
      # Standard pyright (no semantic tokens):
      # command: "pyright-langserver"
      # args: ["--stdio"]
    formatter:
      command: "black"
      args:
        - "--quiet"
  typescript:
    primary:
      command: "typescript-language-server"
      args:
        - "--stdio"
  c_cpp:
    primary:
      command: "clangd"
      args: []
  rust:
    primary:
      command: "rust-analyzer"
      args: []
  java:
    primary:
      command: "jdtls"
      args: []

# Optional extension overrides. Users can add entries under lsp.extension_map in settings.
extension_map:
  cc: "c_cpp"
  cpp: "c_cpp"
  c: "c_cpp"
  h: "c_cpp"
  hpp: "c_cpp"
  jsx: "typescript"
  tsx: "typescript"
  rs: "rust"
  java: "java"
