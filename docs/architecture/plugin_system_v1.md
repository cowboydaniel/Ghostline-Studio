# Plugin System v1

Plugins declare metadata in `plugin.yaml` (name, version, author, license, summary, tags).
A loader validates metadata, registers commands, menus, and docks while isolating errors per plugin.
